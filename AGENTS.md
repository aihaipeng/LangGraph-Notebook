# AGENTS.md

## 目标

以最小、可验证的改动完成用户需求；不做未授权的扩展。

## 工作规则

1. **先理解**：先读相关章节的 notebook、`CheatSheet.md` 与 `LangGraph/To-Do.md`；不确定的 API 行为必须查官方文档或安装包源码求证，不凭记忆臆测。
2. **先计划**：多步骤任务列出简短步骤与验证方式，用任务列表工具跟踪；每完成一步就更新状态。
3. **小范围修改**：只改与需求直接相关的章节文件，遵循各章节已有的编号与命名惯例；不顺手重构或清理无关 notebook。
4. **优先复用**：工具、模型、示例数据优先复用当前章节已有案例；新建案例前先确认无现成可套用的实现。
5. **验证闭环**：动过的 notebook 代码 cell 必须实际执行验证（CLI 环境无 jupyter/IPython，可提取 cell 源码用脚本顺序执行，`display` 等 IPython 专属 cell 跳过并在交付时说明）；真实模型调用验证输出语义正确。流程图（HTML/drawio）改动需截图目检。无法验证时说明原因和剩余风险。
6. **安全操作**：不泄露 `.env` 中的密钥（DEEPSEEK_API_KEY 等）与个人数据；测试脚本放临时目录，用完清理；不使用破坏性命令；尊重并保留用户已有改动（如手动重命名的文件）。
7. **保持可维护**：代码清晰、注释只解释非显然原因；及时删除由本次改动产生的无用导入和变量。
8. **如实交付**：说明改了什么、验证了什么、未完成什么；仓库内引用使用相对路径+章节/节号（notebook 用节名，md 用节号），仓库外使用绝对路径。

## 代码风格

- `print` 统一使用 rich 版本（`from rich import print`），不使用内置 `print`.
- AI 生成的代码案例优先复用当前章节已存在的案例（工具、模型、示例数据等保持一致），不另起炉灶。

## 章节文档（CheatSheet.md）

- 每个章节目录（如 `LangGraph/6_中断/`）维护一份 `CheatSheet.md`：存量目录逐步补齐，新增目录时同步创建。
- 格式二选一：传统文档或 Q&A 形式；同一份内保持统一。
- 内容要求：语言精炼、重点突出，不长篇大论；优先表格与最小代码示例，实测结论标注"实测"。

## 推送前审核

执行推送流程前，按以下清单审核：

1. **代码验证**：修改过的目录中，动过代码的章节内容须确认无误，代码案例可执行。
2. **知识沉淀**：本次更新的知识点、用户的提问、解决的 BUG 已完整记录进对应目录的 `CheatSheet.md`。
3. **To-Do 核对**：审核 `LangGraph/To-Do.md`，已完成的事项打勾，并标注代码来源（文件路径+节号）。

## 沟通

- 默认使用中文，保留代码、命令、路径、日志和 API 字段原文。
- 开始修改前说明方案；工作期间定期报告进展；完成后给出简洁结果和验证信息。
- 未经明确要求不执行 git commit/push；不更新 git config，不强制推送。
