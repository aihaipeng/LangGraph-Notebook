# AGENTS.md

## 目标

以最小、可验证的改动完成用户需求；不做未授权的扩展。

## 工作规则

1. **先理解**：先读相关代码、配置和测试；不确定的内容必须进行调研和提问。
2. **先计划**：多步骤任务列出简短步骤与验证方式，用任务列表工具跟踪；每完成一步就更新状态。
3. **小范围修改**：遵循现有架构和风格，只改与需求直接相关的文件；不顺手重构或清理无关代码。
4. **优先复用**：使用项目已有的库、工具和模式；避免为单一用途增加抽象或依赖。
5. **验证闭环**：改动前后运行相关测试、构建或静态检查；验证命令未知时，先查 README 或构建脚本确认，不臆测；若无法运行，说明原因和剩余风险。涉及前端修改与页面交互的，需要进行视觉回归；涉及多模块修改的，需要进行 E2E 回归。
6. **安全操作**：不泄露密钥和个人数据；不使用破坏性命令；尊重并保留用户已有改动。
7. **保持可维护**：代码清晰、注释只解释非显然原因；及时删除由本次改动产生的无用导入和变量。
8. **如实交付**：说明改了什么、验证了什么、未完成什么；仓库内文件引用使用相对路径+行号，仓库外使用绝对路径。

## 代码风格

- `print` 统一使用 rich 版本（`from rich import print`），不使用内置 `print`；存量代码中的 `from rich import print as rprint` 别名写法保持不变。

## 沟通

- 默认使用中文，保留代码、命令、路径、日志和 API 字段原文。
- 开始修改前说明方案；工作期间定期报告进展；完成后给出简洁结果和验证信息。
- 未经明确要求不执行 git commit/push；不更新 git config，不强制推送。
